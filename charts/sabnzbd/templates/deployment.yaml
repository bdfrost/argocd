apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: {{ .Values.spec.name }}
spec:
  replicas: {{ .Values.spec.replicas }}
  template:
    metadata:
      labels:
        app: {{ .Values.spec.name }}
    spec:
      containers:
      - name: {{ .Values.spec.name }}
        image: {{ .Values.spec.name }}/{{ .Values.spec.name }}
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        env:
        - name: SABNZBD_UID
          value: "1000"
        - name: SABNZBD_GID
          value: "1000"
        volumeMounts:
        - name: {{ .Values.spec.name }}
          mountPath: /datadir
        - name: sabnzbd-downloads
          mountPath: /download
        resources:
          limits:
            memory: 50Mi
            cpu: 100m
          requests:
            memory: 50Mi
            cpu: 10m
      volumes:
      - name: {{ .Values.spec.name }}
        persistentVolumeClaim:
          claimName: {{ .Values.spec.name }}
      - name: sabnzbd-downloads
        hostPath:
          path: /mnt/fileserv/Library