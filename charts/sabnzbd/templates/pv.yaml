apiVersion: v1
kind: PersistentVolume
metadata:
    name: {{ .Values.name }}
spec:
    capacity:
        storage: 500Mi
    accessModes:
        - ReadWriteOnce
    nfs:
        path: /mnt/fileserv/kubernetes/{{ .Values.name }}
        server: 10.0.1.14