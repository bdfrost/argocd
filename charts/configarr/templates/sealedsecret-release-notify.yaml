---
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: release-notify-webhook
  namespace: configarr
spec:
  encryptedData:
    # Seal this with: cat webhook-secret.yaml | kubeseal --format yaml > sealedsecret-release-notify.yaml
    # Then replace this placeholder with the actual sealed value
    webhook-url: PLACEHOLDER
  template:
    metadata:
      name: release-notify-webhook
      namespace: configarr
