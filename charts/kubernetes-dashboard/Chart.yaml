apiVersion: v2
name: kubernetes-dashboard
description: A Helm chart for Kubernetes

type: application
version: 0.1.0 # version: 7.10.0

# export POD_NAME=$(kubectl get pods -n kubernetes-dashboard -l "app.kubernetes.io/name=kubernetes-dashboard-web,app.kubernetes.io/instance=kubernetes-dashboard" -o jsonpath="{.items[0].metadata.name}")
# kubectl -n kubernetes-dashboard port-forward $POD_NAME 8443:8443

dependencies:
  - name: kubernetes-dashboard
    version: 7.10.0
    repository: https://kubernetes.github.io/dashboard
