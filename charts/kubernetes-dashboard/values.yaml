kubernetes-dashboard:
  app:
    mode: 'dashboard'
    nginx:
      enabled: false
    cert-manager:
      enabled: true
  kong:
    proxy:
      http:
        enabled: true
        # service:
        #   type: LoadBalancer
        #   port: 80
      https:
        enabled: true
        service:
          type: LoadBalancer
          port: 443
