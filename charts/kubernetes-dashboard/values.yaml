kubernetes-dashboard:
  app:
    mode: 'dashboard'
    nginx:
      enabled: false
    cert-manager:
      enabled: true
  kong:
    # ingressController:
    #   enabled: false
    proxy:
      type: LoadBalancer
