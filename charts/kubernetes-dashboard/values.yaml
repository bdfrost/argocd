kubernetes-dashboard:
  app:
    mode: 'dashboard'
    nginx:
      enabled: false
    cert-manager:
      enabled: true
    tls:
      enabled: true
      secretName: kubernetes-dashboard-tls
  kong:
    proxy:
      type: LoadBalancer
