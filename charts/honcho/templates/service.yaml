apiVersion: v1
kind: Service
metadata:
  name: {{ include "honcho.name" . }}
  labels:
{{ include "honcho.labels" . | nindent 4 }}
spec:
  selector:
    app.kubernetes.io/name: {{ include "honcho.name" . }}
    app.kubernetes.io/component: api
  ports:
  - name: http
    port: {{ .Values.service.port }}
    targetPort: http
