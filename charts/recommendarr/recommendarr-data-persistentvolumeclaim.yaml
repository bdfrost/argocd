apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: recommendarr-data
  name: recommendarr-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
