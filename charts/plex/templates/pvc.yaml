apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: plex-config-pvc-v5
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: synology-csi-iscsi-delete
  resources:
    requests:
      storage: 250Gi